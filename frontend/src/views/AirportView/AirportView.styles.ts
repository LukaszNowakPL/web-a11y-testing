import {css} from '@emotion/css';

export const mainHeader = css({
    marginBottom: '20px',
});

export const regionsList = css({
    listStylePosition: 'inside',
    paddingLeft: '0px',
    margin: '0px',
});
